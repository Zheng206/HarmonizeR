% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch_auc.R
\name{batch_auc_cal_cv}
\alias{batch_auc_cal_cv}
\title{Cross-validated random forest batch-prediction AUC}
\usage{
batch_auc_cal_cv(
  data_list,
  bat_list,
  k = 5,
  ntree = 100,
  seed = 1,
  metric_prefix = "M"
)
}
\arguments{
\item{data_list}{A list of data frames or matrices, one per metric/modality.
Rows are observations and columns are features.}

\item{bat_list}{A list of batch-label vectors aligned with \code{data_list}, or a
single batch-label vector that will be reused for all metrics.}

\item{k}{Number of cross-validation folds.}

\item{ntree}{Number of trees used by \code{\link[randomForest:randomForest]{randomForest::randomForest()}}.}

\item{seed}{Random seed used for fold creation and random forest fitting.}

\item{metric_prefix}{Prefix used to label metrics in the output.}
}
\value{
A data frame with one row per metric and fold. Columns include
\code{measurement}, \code{fold}, and \code{auc}.
}
\description{
Computes out-of-sample random forest prediction of batch labels separately
within each metric/modality. For multi-class batch labels, the function
reports the macro-average one-vs-rest AUC across batch classes.
}
\examples{
\dontrun{
auc_df <- batch_auc_cal_cv(data_list, bat_list, k = 5, ntree = 100)
}
}
